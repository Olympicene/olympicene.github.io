<style is:global>
	main {
		display: flex;
        flex-wrap: wrap;

		gap: 20px;

		margin: auto;
		width: 50%;
		max-width: 960px;

		line-height: 1.6;
	}

	.left {
		display: flex;
		gap: 20px;
		flex-direction: column;

		flex: 1 1 auto;
		max-width: calc((65% - 6 * 20px) + 4 * 20px);
	}

	.right {
		flex: 1 1 auto;
	}
</style>